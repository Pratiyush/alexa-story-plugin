image: node:18

# Simple CI pipeline that runs install, lint, and tests.
stages:
  - install
  - lint
  - test

cache:
  paths:
    - node_modules/

install:
  stage: install
  script:
    - npm install

lint:
  stage: lint
  script:
    - npm run lint

test:
  stage: test
  script:
    - npm test
